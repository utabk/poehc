[package]
name = "poehc-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for integrating with the PoEHC protocol"

[dependencies]
alloy = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
